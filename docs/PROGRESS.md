# Progress

## Current phase
Phase 2 — tasks 1-11 implemented. Gate G2 mostly PASSES (parse rate, train-song count, scripted-player hit_rate on both 20 random and the 5 densest Expert val songs, all 1.000) but the env-throughput sub-check does not (6.8x measured vs ≥10x required — see Open issues). Debug video for Metallica - One (Expert) rendered to `runs/debug_video_metallica_one_expert.mp4`; **waiting on the user to confirm it before tagging `phase-2-done`**, per their explicit instruction.

## Gate results
| Gate | Status | Numbers | Date |
|---|---|---|---|
| G0 | PASS | `uv run pytest -q`: 3 passed. `torch.backends.mps.is_available()` = True. Song folders: 652 (≥100 required). notes.chart: 306, notes.mid: 347, .sng: 0. | 2026-09-15 |
| Native-difficulty check (pre-Phase-1) | PASS, no plan change | Songs with native Easy AND Medium: 293 (≥150 threshold). | 2026-09-15 |
| G1 | PASS | `uv run pytest -q`: 8 passed. Proofread root IDs (raw): 139,255. Final node set (proofread ∩ annotated): 139,241. Edges (min_syn=5, summed over neuropils): 2,700,429 (39.39% sign −1, 60.61% sign +1). Type pairs: 410,768 (> 300k fallback threshold — resolved via D16 hybrid gain sharing, see log). DNs: 1,303 (≥100 required). Input layer: photoreceptors, 11,118 neurons (≥500 required); lamina fallback would have given 4,730. T4/T5 matched: 12,246. Full breakdown incl. D11-D16 provenance counts in `docs/graph_report.md`. | 2026-09-15 |
| G2 | PARTIAL — 1 sub-check fails | `uv run pytest -q`: 38 passed. Parse rate 652/652 (100% ≥ 90%). Train songs: 453 (≥150). Scripted-perfect-player `hit_rate`: **1.000, 0 overstrums, 0 misses** on both the 20 random Expert val songs AND the 5 densest (by notes/s: Wanderflux 9.98, The Duel 8.17, Mechanical Syndrome 7.48, Revolution Deathsquad 7.40, Epos of Shit 7.24). Random player mean `hit_rate`: 0.0219. Env throughput at batch 64: **6.8x real-time on CPU, below the ≥10x gate** (see Open issues). Human check (debug video) pending user confirmation. | 2026-09-15 |

## Running jobs
(none)

## Log
<!-- newest first: date — task — outcome — deviations — repro command -->
- 2026-09-15 — Phase 2 tasks 6-11: rules, labels, render, retina, sim/env, debug video — Gate G2 mostly PASS, 1 sub-check open —
  - **Timing spot check** (both before and after implementing tasks 6-11, per the user's request): `Guitar Hero III - Legends of Rock/Quickplay/Metallica - One`, Expert, `chart_offset_s=0.0`, `ini_delay_s=0.0` (it's the `.mid`-sourced winner of an exact-duplicate pair from tasks 1-5). First 10 notes (time_s, lanes, sustain_s): 21.940 [1] 0.000; 22.244 [3] 0.000; 22.549 [1] 0.000; 22.853 [2] 1.370; 24.375 [0] 0.000; 24.678 [3] 0.000; 24.981 [0] 0.000; 25.284 [2] 1.362; 26.799 [1] 0.000; 27.100 [3] 0.000. User confirmed this against the song both times.
  - **New files:** `flyhero/game/{labels,rules,render,retina,sim,env}.py`, `scripts/gate_g2_check.py`, `bench/bench_env.py`, `tests/{test_labels,test_rules,test_retina}.py`. `flyhero/game/song.py` gained `merge_close_notes`/`load_song_merged` (load-time-only chord merge, never cached). `flyhero/game/ingest.py` gained per-difficulty chord-merge and max-simultaneous-open-window reporting. `flyhero/connectome/build_graph.py` gained a `side_id` array (0=left/1=right/2=center/3=na) in `graph.npz` — metadata, not topology (invariant 3 unaffected) — needed because retina.py must split photoreceptors into left/right eye groups and the raw annotations don't carry a "side" field per node in `graph.npz` as originally built; rebuilt `graph.npz`/`graph_shuffled.npz` (4s, foreground).
  - **Labels** (`labels.py`): fret target = midpoint-partitioned segments (phantom neighbors at t=0 and t=duration_s) holding each note's lane_mask until `min(segment_end, time_s + max(sustain_s, hit_window_s))`. The `max(sustain_s, hit_window_s)` floor (not just `sustain_s`, per the user's approved fix) guards against frame-quantization rounding a zero-sustain note's own strum frame to just after `time_s`, past a too-short hold — verified with a dedicated test and confirmed necessary by the real densest-song regression test below. Strum = nearest frame to each note's time; two notes rounding to the same frame raises (should be impossible post-merge).
  - **Rules** (`rules.py`): stateful per-song `RuleEngine.step()` — a strum credits the *earliest* unhit, exactly-lane-matching, window-open note; misses resolve lazily when a note's window closes (`t > time_s + hit_window_s`), not the instant a strum fails to match. `scripted_perfect_actions()` deliberately reuses `labels.compute_frame_labels()` rather than a hand-rolled per-note hold window — an independent hold window would OR together on overlapping dense-trill notes, producing a held mask matching neither note's lane_mask and turning hits into overstrums; reusing the labels *is* the check that the label design is playable.
  - **Chord-merge / max-open-window real numbers** (`uv run python -m flyhero.game.ingest --config configs/game.yaml`, 20.0s, foreground): only **2** notes absorbed by the 1/60s load-time merge across the entire library (both Expert) — real charts essentially never have notes this close. Max simultaneously-open ±hit_window_s windows, post-merge: Easy 2, Medium 2, Hard 3, **Expert 8** — library-wide max **8**, confirming `configs/game.yaml`'s `max_open_notes_capacity: 8` is the measured real number, not a guess.
  - **Retina anatomy** (`retina.py`) — the substantial part of this task:
    - Column/ommatidium IDs: **not available** — checked the full 31-field v3.1.0 annotations header directly, nothing column/ommatidia-related.
    - Axis convention **empirically confirmed**, not assumed: `side_id` shows X cleanly separates left (mean 314,431 nm) from right (729,752 nm) neurons (std ~100k, i.e. a real bimodal split, not noise). Y=dorsal-ventral: the most dorsal known class (endocrine/pars-intercerebralis) has the single lowest mean Y (z-score −1.75) of any `super_class`; Z=anterior-posterior: ascending/sensory_ascending neurons (entering via the neck) have the highest mean Z (+1.07/+1.09).
    - First attempt (naive: vertical=raw Y, horizontal=raw Z) failed badly: R1-6/R8-sharing-a-Mi1 median distance = **136.7 lattice spacings** (need <2). Diagnosis: PCA on real per-side R1-6/R8 positions shows PC1 is Y-dominated (~0.99 loading, confirms the vertical axis) but **PC2 is a genuine mix of X and Z** (R1-6 loadings ≈[-0.65,-0.13,-0.75], R8 ≈[0.90,0.09,0.43]) — the lamina/medulla are curved, tilted neuropils, not axis-aligned. Fixed with per-group PCA (fit once per (group,side) population, sign fixed anatomically via correlation with Y/Z, never PCA's arbitrary sign) → median dropped to **4.95**.
    - Second bug found: the "lattice spacing" reference (R1-6's own nearest-neighbor distance) was measuring *within-column* spacing (the ~6 R1-6 cells of one ommatidium, converged via neural superposition, sit almost on top of each other — ~4000 R1-6 cells/side vs only ~800 real columns) rather than *between-column* spacing. Switched to L1 (≈one cell per lamina cartridge, ~800/side) for the lattice-spacing reference → median dropped to **1.63 lattice spacings**, under the 2.0 threshold. (Both fixes came from actually inspecting real Mi1-column member positions and PCA loadings, not from adjusting the threshold or the data.)
    - Medulla chiasm flip: calibrated empirically (tried both orientations, kept the one minimizing the Mi1-based median distance) rather than assumed from anatomy alone — chose **flip=True** (median 1.626 vs 1.634 unflipped), consistent with the user's stated "front-to-back inverted" but derived from data, not memory.
    - The 10 soma-backed photoreceptors (7 R7, 2 R8, 1 R1-6, from the earlier NaN-position work) are excluded from retina input entirely — zero current — logged via `PhotoreceptorMap.n_excluded == 10`.
    - Both eyes see the same full [0,1]² frame (PLAN's original spec), split so each eye's field occupies its own half (right eye → [0.5,1], left eye → [0,0.5], mirrored) with each eye's frontal/binocular direction near the shared midline and its peripheral direction at the outer edge — a reading of "each eye's field on its own side of the screen" that isn't 100% unambiguous from the instruction alone; flagged for the user to correct from the debug video if they meant something else.
    - EMA initialized with the first frame (`ema = intensities[0].copy()` before the loop) — verified by test that `current[0]` is exactly 0.
  - **Throughput optimization** (Gate G2 requires ≥10x real-time at batch 64 on CPU): naive per-env-per-frame implementation measured **0.7x** — far short. Optimized in order, each step measured: (1) batch the retina blur/sample across all 64 envs instead of a Python loop calling per-env → 2.1x; (2) switch photoreceptor sampling from bilinear to nearest-neighbor + float32 (frame is already Gaussian-blurred, so sub-pixel interpolation added ~10x the per-sample cost for negligible quality) → 5.1x; (3) cache `render_frame`'s static background/lane-lines/strikeline per `frame_size` instead of redrawing every call → 5.9x; (4) precompute nearest-pixel sample indices once per env (not every frame) and use a dense gather-then-multiply-by-a-0/1-mask instead of a sparse boolean-indexed assignment (measured ~35% faster) → **6.8x**. Tried and rejected: thread-pool-splitting the batch gather (no improvement, thread overhead cancels any GIL-release benefit at this size). **Still below the ≥10x gate** — see Open issues; correctness is unaffected (all 38 tests still pass, Gate G2's hit_rate checks unaffected since they don't go through the env).
  - **Debug video**: rendered `Metallica - One` (Expert) to `runs/debug_video_metallica_one_expert.mp4` (448s song, 5.5 MB, 3 panels: highway | fret-target-vs-scripted-action with hit/miss/overstrum markers | retina input at each photoreceptor's image position). Spot-checked a frame during a lane-2 sustain note: highway/target/action panels agree, and the retina panel shows a bright, roughly mirror-symmetric cluster in both eye halves at the expected mid-height — visually plausible, but the user's own review of the full video is the actual check per PLAN's Gate G2 "human check" and their explicit request to confirm before tagging `phase-2-done`.
  - Repro: `uv run pytest -q` (38 passed); `uv run python -m flyhero.connectome.build_graph --config configs/connectome.yaml` then `.shuffle`; `uv run python -m flyhero.game.ingest --config configs/game.yaml`; `PYTHONPATH=. uv run python scripts/gate_g2_check.py`; `PYTHONPATH=. uv run python bench/bench_env.py`; `PYTHONPATH=. uv run python -m flyhero.game.sim --config configs/game.yaml --video babaf336d9a4ddb2 Expert --out runs/debug_video_metallica_one_expert.mp4`.
- 2026-09-15 — Phase 2 tasks 1-5: song model, `.chart`/`.mid` parsers, ingest, dedupe, split — real-library run complete, not yet Gate G2 —
  - **New files:** `flyhero/utils/text.py` (shared lenient text decoder), `flyhero/game/{song,tempo,chart_parser,midi_parser,ingest}.py`, `configs/game.yaml`, `tests/{test_song_parsers,test_ingest}.py`.
  - **Real bugs found and fixed before running on the full library:** (1) 27/652 `song.ini` files fail to parse under a naive utf-8/cp1252/latin-1 decode chain — some are UTF-8-with-BOM (`﻿[song]`), some are genuine UTF-16 (Windows Notepad "Unicode" saves, null-interleaved bytes). Fixed with a BOM/UTF-16-sniffing `read_text_lenient()` (also used to refactor `library/scan.py`'s equivalent private helper); verified 652/652 parse after the fix. (2) Every real `song.ini`'s `charter` field is wrapped in Unity rich-text markup (`<color=#rrggbb>Name</color>`); stripped before storing/comparing. (3) An early version of `midi_parser.py` re-scanned the whole `PART GUITAR` track once per difficulty, quadruple-counting `sysex_ignored`/`ignored_notes`; fixed with a single pass building all four difficulties' note arrays together (`ignored_counts` is therefore file-scoped, duplicated identically onto each returned `ParsedDifficulty` — documented in the module docstring).
  - **User-approved deviations from the Phase 2 plan** (see prior turn's plan message for the ambiguities raised): star power (`S`) ignored/counted in `.chart` same as `.mid`; `sustain_min_beats` applied to both formats; `duration_s` = song.ini `song_length` if present and > 0, else last-note-end; open-note exclusion is per-`(song, difficulty)`, not per-song. Exact-duplicate (`raw (artist,title)`) tie-break: most surviving difficulties → most Expert notes → sorted `rel_path`; every drop logged with both sides' numbers. **Split changed to group-level**: songs are grouped by *normalized* `(artist,title)`; all members of a group (including near-duplicates, which are reported but never merged) get the same train/val/test split, so two charts of the same underlying song can't leak across splits; stratum = `native_easy_medium` if *any* group member has both native Easy and Medium post-filter, else `expert_only`. `.mid`: `note_on velocity=0` treated as `note_off`; tempo map built from `set_tempo` in *all* tracks (warns if found outside track 0). `Song` gained `chart_offset_s` (from `.chart`'s `Offset`, 0.0 for `.mid`) and `ini_delay_s` (from `song.ini`'s `delay`), both recorded and unused for training.
  - **Real-library ingest run** (`uv run python -m flyhero.game.ingest --config configs/game.yaml`, 18.9s wall-clock, foreground): 652 folders considered, **0 parse failures**. **83 open-note exclusions** (all `[Expert]`; 79 of those left the song with zero surviving difficulties and it was dropped entirely, since most are Expert-only custom charts — the other 4 songs kept their other native difficulties). **5 exact-duplicate drops** (e.g. `Guitar Hero - Metallica/Metallica - One` dropped in favor of `Guitar Hero III - Legends of Rock/Quickplay/Metallica - One`, 2146 vs 2207 Expert notes). **11 near-duplicate groups** reported (all "(Co-op)"-suffix pairs except one, e.g. `foo fighters / the pretender`) — none merged, all confirmed to land in one split each. **568 songs written** (1434 `<song_id>_<diff>.npz` files) → split **453 train / 55 val / 60 test** (79.8% / 9.7% / 10.6%, close to the 80/10/10 target given group-level rounding). Full per-split × per-stratum × per-difficulty breakdown, every exclusion/drop/near-dup group, and parse-failure list (0) in `docs/library_report.md`'s new "Ingest results (Phase 2)" section.
  - Not yet Gate G2: that requires tasks 6-11 (`rules.py`, `labels.py`, `render.py`, `retina.py`, `sim.py`/`env.py`, their tests, the scripted-perfect-player check, and the human video check), not started.
  - Repro: `uv run pytest -q` (24 passed); `uv run python -m flyhero.game.ingest --config configs/game.yaml` → updates `data/processed/songs/`, `data/processed/splits.json`, and appends to `docs/library_report.md`.
- 2026-09-15 — Pre-Phase-2 tiny fix: extended the NaN-position regression test to `graph_shuffled.npz`, identified the 10 soma-backed photoreceptors —
  - Parametrized `test_real_graph_has_no_nan_positions` (`tests/test_connectome.py`) over both `data/processed/graph.npz` and `data/processed/graph_shuffled.npz` (skipped if either is missing). `shuffle_graph()` copies every array from `graph.npz` unchanged except `post`, so this is a regression guard against a future shuffle-script change silently dropping or corrupting position fields, not a new finding — both files currently carry identical, NaN-free position data.
  - Pos source of the 10 non-anchor (`pos_source_id == 0`, soma) photoreceptors, by root ID and type: 7× R7 (720575940606471345, 720575940611818594, 720575940614944209, 720575940619203909, 720575940625168363, 720575940628221256, 720575940639580238), 2× R8 (720575940610415299, 720575940633166929), 1× R1-6 (720575940631014656). Matches `graph_meta.json`'s `pos_source_counts_photoreceptor`: `{soma: 10, anchor: 11108, none: 0}`.
  - Repro: `uv run pytest -q` (10 passed).
- 2026-09-15 — Post-Phase-1: verified the 10 soma-backed photoreceptors, added a NaN-position regression test —
  - Confirmed, against the raw annotations TSV, that the 10 photoreceptors with `pos_source_id == 0` (soma) are real: distinct root IDs (mostly R7, plus a couple R1-6/R8), each with populated `soma_x/y/z` at plausible in-volume voxel coordinates close to but distinct from their `pos_x/y/z` anchor — not an artifact of the earlier bug fix, just a small minority of photoreceptors that happen to have a proofread soma inside FAFB. Confirmed `np.isnan` is `False` everywhere in `graph.npz`'s `pos_voxel` and `pos_nm` (all 139,241 nodes), and no node has `pos_source_id == 2` ("none").
  - Added `test_real_graph_has_no_nan_positions` (`tests/test_connectome.py`, skipped when `data/processed/graph.npz` doesn't exist so `pytest -q` stays fast/portable elsewhere) asserting: no NaN anywhere in `pos_voxel`/`pos_nm`; no node with `pos_source_id == 2`; and among photoreceptors, both the anchor-fallback group (dominant) and the soma group (10 neurons) are non-empty and NaN-free. Also added a general (fixture-based, always-run) no-NaN assertion to `test_build_graph_index_round_trip`.
  - Repro: `uv run pytest -q` (9 passed, including the real-data test since `data/processed/graph.npz` exists on this machine).
- 2026-09-15 — Post-Phase-1: gain-sharing decision (D16) + a real position bug found while answering the user's follow-up questions — no plan blockers, PLAN.md/DECISIONS.md updated —
  - **Bug fix (found via the user's question about photoreceptor soma vs. anchor positions):** `build_graph.py`'s position logic chose soma-vs-anchor once for the whole `soma_x/y/z` column set, not per neuron. Since the column *exists* for every neuron (just null row-by-row for ~11.1k of them), this left 11,108 of 11,118 photoreceptors with **NaN** positions in `graph.npz` instead of falling back to their `pos_x/y/z` anchor point — confirmed both in the raw TSV (`soma_x/y/z` null, `pos_x/y/z` populated, e.g. root_id 720575940620132116: pos (56218, 76524, 4129)) and in the already-built `graph.npz` (11,108/11,118 NaN rows) before fixing. Fixed with a proper per-neuron fallback (`resolve_positions()` in `build_graph.py`); added `pos_source_id` to `graph.npz` (0=soma/1=anchor/2=none) and a regression test (`test_build_graph_index_round_trip`, neuron 1 given a null soma + populated anchor). Rebuilt `graph.npz` and `graph_shuffled.npz`; re-verified the shuffle still preserves degree/sign/syn_count exactly. Post-fix: 118,100 neurons use soma, 21,141 use the anchor fallback (11,108 of those are photoreceptors, as expected since retinal somata sit outside FAFB), 0 have neither.
  - Type-pair size distribution computed (`docs/graph_report.md`): at min-edges thresholds 1/2/5/10/20, pairs at/above cover 100%/93.85%/75.27%/66.68%/61.76% of edges respectively.
  - **D16 logged** (PLAN.md Phase 3, DECISIONS.md): gain sharing configurable as `fine` (410,768 params) / `coarse` (`pre_type, post_super_class`, 22,212 params) / `hybrid` (default: a pair keeps its own gain at ≥ k edges, else the coarse bucket). k=2 chosen as the largest threshold with fine-pair edge coverage ≥ 90% (actual: 93.85%; k=3 drops to 85.88%) → 244,824 fine pairs + 18,925 coarse buckets = **263,749 total gain parameters**. ES (Phase 5 stretch) forced to `coarse`. Mechanism itself is deferred to Phase 3's `rate_model.py`; Phase 1 only needed to report the numbers, since `graph.npz` already carries `pair_id`/`type_id`/`super_class_id`, sufficient to derive any of the three schemes without a rebuild.
  - Repro: `uv run pytest -q`; `uv run python -m flyhero.connectome.build_graph --config configs/connectome.yaml`; `uv run python -m flyhero.connectome.inspect --config configs/connectome.yaml` → `docs/graph_report.md`; `uv run python -m flyhero.connectome.shuffle --config configs/connectome.yaml`.
- 2026-09-15 — Phase 1: connectome graph — PASS (G1) —
  - Downloaded (background, resumable via HTTP Range) from Zenodo record 10676866: `proofread_connections_783.feather` (852,022,274 B) and `proofread_root_ids_783.npy` (1,114,168 B); from `flyconnectome/flywire_annotations` pinned at tag `v3.1.0`: `Supplemental_file1_neuron_annotations.tsv` (31,718,505 B). Did not download `flywire_synapses_783.feather` (~9.5 GB) or the two `per_neuron_neuropil_count_*.feather` files — not needed by any Phase 1/3 task.
  - Deviation from PLAN.md found and corrected during implementation: PLAN's download step named a standalone "v783 neurotransmitter predictions" file that does not exist. Per the user's approved D11-D13, NT sign now comes from the annotations' per-neuron `top_nt` (fallback: synapse-weighted mean of a neuron's outgoing per-edge NT columns from the connections table; photoreceptors forced to histamine; dopamine/serotonin/octopamine +1 for v1).
  - Real-data bug caught before Gate G1 was finalized: `top_nt` in the live v3.1.0 TSV spells transmitters out in full (`acetylcholine`, `glutamate`, `dopamine`, `serotonin`, `octopamine`, `gaba`), not the short codes (`ach`, `glut`, ...) used by the connections table's per-edge columns. An initial run silently defaulted every `glutamate`/`acetylcholine`/etc. neuron to sign +1 via `replace_strict`'s default, which would have wrongly made all directly-predicted glutamatergic neurons excitatory. Fixed by normalizing `top_nt` to the short codes before sign lookup (`NT_NAME_ALIASES` in `build_graph.py`); added `test_top_nt_full_word_spellings_normalize_to_correct_sign` to catch a regression. Also found: empty-string `top_nt` values (602 raw rows) are not null in the polars-parsed column and were not triggering the fallback path until explicitly checked for.
  - D11-D15 counts (full breakdown in `docs/graph_report.md`): `top_nt` used directly for 114,935 neurons; synapse-weighted fallback used for 23,387; no data at all (defaulted to ACh/+1) for 919. Histamine override applied to 11,118 photoreceptors (originally predicted: 6,958 ach, 2,863 glut, 769 gaba, 377 ser, 116 oct, 35 da). Type-ID fallback chain: 137,714 neurons at `cell_type`, 2 at `hemibrain_type`, 1,358 at `cell_class`, 167 singleton (9,028 distinct types total).
  - Photoreceptor/lamina/T4-T5 `cell_type` spellings guessed in the plan (`R1-6`/`R7`/`R8`, `L1`/`L2`/`L3`, `T4a-d`/`T5a-d`) were verified exactly correct against the live TSV before being used.
  - Shuffle (`connectome/shuffle.py`, double-edge-swap, factor 10x edges) verified on the real graph: in-degree, out-degree, sign, and syn_count arrays identical to `graph.npz`; 99.99% of postsynaptic targets changed; 0 duplicate edges; 0 self-loops introduced. Took 86s on 2.7M edges (under the 2-minute background threshold, ran in foreground).
  - Repro: `uv run pytest -q`; `uv run python -m flyhero.connectome.download --config configs/connectome.yaml`; `uv run python -m flyhero.connectome.build_graph --config configs/connectome.yaml`; `uv run python -m flyhero.connectome.inspect --config configs/connectome.yaml` → `docs/graph_report.md`; `uv run python -m flyhero.connectome.shuffle --config configs/connectome.yaml`.
- 2026-09-15 — Pre-Phase-1: per-difficulty note-range check on `.mid`-only songs — decision rule satisfied, no plan change —
  - Extended `flyhero/library/scan.py` to fully parse `PART GUITAR` note ranges (Easy 60-64, Medium 72-76, Hard 84-88, Expert 96-100) for the 346 `.mid`-only songs (`mid_guitar_difficulties`), and combined with the existing `.chart`-section counts into per-difficulty **song** counts (not file counts): Easy 294, Medium 294, Hard 294, Expert 652.
  - Decision rule (user-specified): amend Phase 2 to add `game/reduce.py` (derive Easy/Medium/Hard from Expert) only if fewer than 150 songs have **native** Easy AND Medium. Actual: **293** songs have both natively — rule satisfied, **no PLAN.md change**. (Native Easy/Medium coverage in this library comes almost entirely from `.mid` files — only 8 of 306 `.chart` files have non-Expert sections — but that's more than enough real lower-difficulty data for the Phase 3 curriculum.)
  - Clip-corruption impact check (`check_clip_corruption_note_impact`): of 347 `.mid` files, 123 need `clip=True` to parse at all (122 `.mid`-only + the 1 song with both `.chart` and `.mid`). Traced each failure to its exact origin in mido's parser: **all 123 fail inside `read_sysex`, 0 inside `read_message`** — the out-of-range bytes are confined to proprietary SysEx payloads (e.g. Harmonix chart metadata), never inside a `note_on`/`note_off` message. So **0 clamped values land in the 60-100 guitar note range**; `clip=True` has no effect on note/difficulty detection accuracy for this library.
  - Repro: `uv run pytest -q`; `uv run python -m flyhero.library.scan --config configs/paths.yaml` → `docs/library_report.md`.
- 2026-09-15 — Phase 0 scaffold + library inventory — PASS (G0) —
  - Deviations from PLAN.md: added `flyhero/utils/` (config.py, run.py) to the repo layout — not in the original diagram (logged as D10 in DECISIONS.md). `scan.py` walks the library recursively (it is not flat: songs sit one level under 5 setlist folders) and skips hidden/temp directories (found 1: `Guitar Hero - Metallica/.tmp.drivedownload`, an empty Google-Drive-sync artifact).
  - Cloud-storage check: 0 zero-byte files, 0 read errors in a 40-file content-read sample — the library is fully local, not cloud placeholders. No abort needed.
  - Library counts: 652 song folders (song.ini), 306 with notes.chart, 347 with notes.mid, 0 `.sng`, 1 folder with both chart and mid, 0 folders with neither.
  - Guitar-part availability: of 306 `.chart` files, all 306 have `[ExpertSingle]`, but only 8 each have `[EasySingle]`/`[MediumSingle]`/`[HardSingle]` (most custom charts in this library are Expert-only). Of 346 `.mid`-only songs, all 346 have a `PART GUITAR` track (required `mido.MidiFile(..., clip=True)` — 122 of them have out-of-range MIDI data bytes that mido rejects by default; this is a known quirk of game-ripped `.mid` files, not corruption). Overall: 652/652 songs have a detected guitar part in at least one difficulty.
  - Size breakdown: chart-related files (.chart/.mid/.ini) 49.7 MB, audio (.ogg/.mp3/.opus) 10.6 GB, video backgrounds (.webm) 446.7 MB.
  - Repro: `uv run pytest -q`; `uv run python -m flyhero.library.scan --config configs/paths.yaml` → `docs/library_report.md`.

## Open issues
- The `.chart`-only Expert skew noted after Phase 0 was resolved by the native-difficulty check above: 293 songs have native Easy+Medium once `.mid` files are included, well over the 150 threshold, so no curriculum-data risk.
- ~~Phase 2: stratify the song train/val/test split by "has native Easy/Medium" vs. "Expert-only"~~ — **Resolved** in the Phase 2 tasks 1-5 log entry above: `ingest.py` stratifies by `native_easy_medium` vs `expert_only` at the (normalized-duplicate) group level.
- ~~Phase 2: before deduping by (artist, title), normalize both fields... write suspected duplicates to `docs/library_report.md`~~ — **Resolved** above: near-duplicate groups (11 found) are reported, never merged, and (per user instruction) forced into one split per group so they can't leak across train/test.
- ~~Phase 2 tasks 6-11 not started~~ — **Done**, see the tasks 6-11 log entry above.
- **Needs the user's decision:** Gate G2's env throughput is **6.8x real-time at batch 64 on CPU, below the ≥10x requirement**, after four rounds of measured optimization (naive 0.7x → 6.8x; see the log entry for what was tried, including a rejected thread-pool attempt). The remaining cost is the per-frame photoreceptor gather (~11,108 points × batch 64, ~0.66ms/step) plus the Gaussian blur and per-env `render_frame` calls — genuinely hard to shrink further in pure NumPy at this problem size without either reducing photoreceptor count (would deviate from Phase 1's locked input-layer choice) or a GPU/torch-vectorized rewrite of the retina/render path (real engineering effort, not attempted here). All correctness gates pass regardless (hit_rate=1.000 on both random and densest val songs; 38/38 tests). Options: (a) accept this as a known Phase 2 limitation and revisit if/when Phase 3's brain forward pass (which will dominate total step time) makes it moot, (b) invest in a GPU/torch retina pipeline now, (c) something else. Have not tagged `phase-2-done` pending this and the video confirmation.
- Debug video panel 3 ("each eye's field on its own side of the screen") was implemented as a literal left/right screen-half split with the frontal/binocular direction near the shared midline — this reading of the instruction wasn't unambiguous; check it against the rendered video and correct if a different orientation was meant.
